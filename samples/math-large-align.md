$$
\begin{align}
    v + w & = 0  & \text{Given} \tag 1 \\
       -w & = -w + 0 & \text{additive identity} \tag 2 \\
   -w + 0 & = -w + (v + w) & \text{equations $(1)$ and $(2)$} \\
\end{align}
$$

# Oversized formula interaction

The aligned display above deliberately exceeds the calculator viewport. Press
Enter or click the touchpad to focus it, pan with Left/Right or a horizontal
swipe, and press Esc or Enter to return to reading.

Bold italic command glyphs: $\imath + \jmath$.

Latin formula text: $\text{Latin Modern Math annotation}$.

CJK fallback label: $\text{公式注释}$.

$$
\begin{aligned}
f(x_1,\ldots,x_n)
  &= \sum_{i=1}^{n}\frac{\alpha_i x_i^2 + \beta_i x_i + \gamma_i}
                            {1 + \sqrt{x_i^2 + \delta_i}} \\
  &\quad + \text{a deliberately long explanatory annotation for pan testing}
\end{aligned}
$$
